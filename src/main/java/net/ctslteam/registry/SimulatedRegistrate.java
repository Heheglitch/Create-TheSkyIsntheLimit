package net.ctslteam.registry;

import net.minecraft.resources.ResourceLocation;

public class SimulatedRegistrate extends dev.simulated_team.simulated.registrate.SimulatedRegistrate {
    public SimulatedRegistrate(ResourceLocation initialSection, String modId) {
        super(initialSection, modId);
    }
}
